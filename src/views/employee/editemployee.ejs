<div class="d-flex justify-content-between flex-wrap flex-md nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h3 class="h2">작업자 정보 수정 <%= employee.name %></h3>

    <div class="btn-toolbar mb-2 mb-md-0">
        <div class="btn-group me-2">
            <button class="btn btn-sm btn-outline-secondary">?</button>
        </div>
    </div>
</div>    

<div class="col py-3">
    <div class="row">
        <div class="col">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/employee">작업자</a></li>
                    <li class="breadcrumb-item active"><%= employee.name %></li>
                </ol>
            </nav>
        </div>
        <div class="col text-end fw-lighter">
            <b>마지막 업데이트: <%= new Date(employee.updatedAt).toUTCString() %></b> 
            <b>UserId:</b> <%= employee._id %>
        </div>
    </div>
</div>






<form action="/edit/<%= employee._id %>?_method=PUT" method="POST">

    <div class="row form-group mb-4">

        <div class="col">
            <label for="name">이름</label>
            <input type="text" class="form-control" id="name" name="name" value="<%= employee.name %>" placeholder="Name">
        </div>

        <div class="col">
            <label for="sex">성별</label>
            <input type="text" class="form-control" id="sex" name="sex" value="<%= employee.sex %>" placeholder="Sex"
            required>
        </div>
    </div>
<!-- 거주지 -->
    <div class="row form-group mb-4">

        <div class="col">
            <label for="local">거주지</label>
            <input type="text" class="form-control" id="local" name="local" value="<%= employee.local %>" placeholder="Local"
            required>
        </div>
<!-- 주민번호 -->
        <div class="col">
            <label for="RRN">주민번호</label>
            <input type="text" class="form-control" id="RRN" name="RRN" value="<%= employee.RRN %>" placeholder="RRN"
            req>
        </div>
    </div>
<!-- 전화번호 -->
    <div class="row form-group mb-4">

        <div class="col">
            <label for="phonenumber">전화번호</label>
            <input type="text" class="form-control" id="phonenumber" name="phonenumber" value="<%= employee.phonenumber %>" placeholder="Telephone"
            required>
        </div>
    </div>

    <div class="row form-group mb-4">
        <button type="submit" class="btn btn-primary">작업자 업데이트하기</button>


        <button type="button" class="btn btn-danger" data-bs-toggle="modal" id="deleteButton" 
        data-bs-target="#deleteModal">작업자 삭제하기</button>
    </div>
</form>

<div class="modal fade" tabindex="-1" role="dialog" id="deleteModal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <div class="modal-title">You are about to remove a employee record.</div>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>
                    이는 작업자의 기록을 제거할 것 입니다 <b class="fw-bold"><%= employee.name %></b><br/>
                    확실합니까? 
                </p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">닫기</button>

                <form action="/edit/<%= employee._id %>?_method=DELETE" method="POST" class="position-relative">
                    <button type="submit" class="btn btn-primary">네, 삭제하겠습니다</button>
                </form>

            </div>
        </div>
    </div>
</div>